query Germany {
  country(code: "DE") {
    name
    continent {
      name
    }
  }
}

query Country($countryCode: String!) {
  country(code: $countryCode) {
    name
    continent {
      name
    }
  }
}
