query Germany {
  country(code: "DE") {
    name
    continent {
      name
    }
  }
}

query Country($countryCode: ID!) {
  country(code: $countryCode) {
    name
    continent {
      name
    }
  }
}
