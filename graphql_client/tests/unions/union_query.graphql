query MyQuery {
    names {
        __typename
        ...on Dog {
            name
        }
        ...on Person {
            firstName
            lastName
        }
        ...on Organization {
            title
        }
    }
}
